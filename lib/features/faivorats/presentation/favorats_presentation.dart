import 'package:flutter/material.dart';

class FavoratsPresentation extends StatelessWidget {
  const FavoratsPresentation({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
